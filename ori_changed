Ori Change Log - March 7, 2026

This file summarizes everything changed today.

1) Password Features

- Added "Change Password" flow (frontend + backend):
  - User enters current password + new password + confirm new password.
  - If current password is correct, password is updated.
  - If current password is wrong, user gets an error message.

- Backend:
  - Added protected endpoint: PUT /api/users/change-password
  - Verifies current password before saving new password.

- Frontend:
  - Added "Change Password" button in Profile page.
  - Added dialog with fields + validation + success/error messages.

- Forgot password:
  - Forgot password flow exists with POST /api/users/forgot-password.
  - Sends temporary password by email (nodemailer-based flow in user controller).


2) Meal Planning UI Flow

- Updated Plan Meal behavior:
  - Clicking "Plan Meal" opens a text dialog first.
  - User can write what kind of meal they want.
  - Clicking "Generate Meal" sends the request and starts meal generation.

- Removed instruction that forced user to write in English.


3) Meal Generation Logic Improvements

- Connected user free-text request to generation:
  - meal_request_note is now passed from frontend to backend.
  - meal_request_note is now injected into the LLM prompt.

- Added persistent preference note support in generation payload:
  - nutrition_preferences_note is now sent when available.
  - nutrition_preferences_note is injected into the LLM prompt.

- Result:
  - Meal generation now considers:
    - macro targets/remaining macros
    - liked/disliked foods
    - meal request text
    - nutrition preference note


4) Nutrition Action Buttons Changes

- Split previous action area into 3 buttons:
  - Rate Food (existing swipe game flow)
  - Tell Me What You Like (new)
  - Plan Meal

- Added "Tell Me What You Like" dialog:
  - Text input for user nutrition priorities
  - Examples: high protein, low sugar, quick meals, budget-friendly
  - Saved in page state and sent during meal generation


5) Rate Food UX Change

- Added a visible "Back" button at the top of the Rate Food overlay.
- User can exit the swipe flow immediately and return to previous page.


6) Nutrition Dataset / User Schema Expansion

- Extended User schema near liked_foods/disliked_foods with a structured block:
  nutrition_preferences:
  - hard_restrictions
    - diets, allergies, medical_restrictions, forbidden_ingredients, notes
  - soft_likes
    - cuisines, foods, notes
  - soft_dislikes
    - cuisines, foods, notes
  - budget_preferences
    - currency, daily_budget, weekly_budget, expensive_days, notes
  - rule_based_preferences
    - cheat_meals_per_week, cheat_days, special_rules, notes
  - practical_constraints
    - max_prep_time_minutes, cooking_skill, equipment, meals_per_day, batch_cooking, notes

- Added in both model files:
  - server/models/User.js
  - ai-service/models/User.js

- Design decision:
  - Kept liked_foods/disliked_foods as explicit item-level preferences (from swipe game).
  - Added soft_likes/soft_dislikes for general preference text/cuisines/food groups.


7) Popup Reset Behavior

- Updated "Tell Me What You Like" popup behavior.
- After saving text, opening the button again now starts with a clean empty input.
- Saved preference is still kept in nutritionPreferencesNote for meal generation.
- Only the visible input box is reset on each open.


8) Nutrition Preference Extraction + Update Pipeline (Implemented)

- Added complete backend pipeline to convert free-text into partial schema updates:
  - extraction service
  - normalization layer
  - conflict detector
  - schema validator
  - merge logic (partial deep merge + array dedupe)
  - orchestrator pipeline

- New protected endpoint:
  - PUT /api/users/nutrition-preferences/extract
  - Extracts from text, validates, merges with existing values, detects conflicts, saves.
  - Returns partial_update + conflicts + merged nutrition_preferences.

- Frontend integration:
  - "Tell Me What You Like" Save now calls the new endpoint.
  - Shows backend error if save fails.
  - Save button has loading/disabled state during request.

- Unit tests added with realistic examples:
  - Mixed Hebrew-English extraction.
  - Partial update behavior.
  - Merge + dedupe behavior.
  - Conflict detection behavior.
  - Schema validation failures.
  - Ensure no liked_foods/disliked_foods macro population from raw text.


9) Key Files Updated

- server/services/nutritionPreferences/normalizationService.js
- server/services/nutritionPreferences/extractionService.js
- server/services/nutritionPreferences/normalizationLayer.js
- server/services/nutritionPreferences/conflictDetector.js
- server/services/nutritionPreferences/validator.js
- server/services/nutritionPreferences/mergeService.js
- server/services/nutritionPreferences/index.js
- server/controllers/userController.js
- server/routes/userRoutes.js
- server/tests/nutritionPreferencesPipelineService.test.js
- src/pages/NutritionDemo.jsx


10) Status Checkpoint (Latest)

- Confirmed end-to-end extraction/update pipeline is in place and active.
- Confirmed "Tell Me What You Like" opens with clean input every time.
- Confirmed frontend save action calls backend extraction endpoint before generation.
- Confirmed tests pass for the new nutrition-preferences pipeline.


11) Auto Refresh Meal After "Tell Me What You Like" Save

- Updated `src/pages/NutritionDemo.jsx` so that after extraction/save succeeds:
  - The frontend reads `nutrition_preferences` from the backend response.
  - Stores it in local state as structured preferences.
  - Immediately triggers `requestMealPlan` to regenerate today's meal.

- Updated meal request payload behavior:
  - `requestMealPlan` can now include `nutrition_preferences` (structured object).
  - Payload still includes optional `nutrition_preferences_note` and `meal_request_note`.
  - This ensures the nutrition AI re-reads all latest structured preference data right after save and refreshes the current meal suggestion.


12) Save Preferences Then Ask Before Meal Plan Update

- Changed the "Tell Me What You Like" save flow in `src/pages/NutritionDemo.jsx`:
  - Saving preferences now updates data only (no immediate meal generation).
  - After save, a confirmation popup asks: "Do you want to update your meal plan?"
  - If user selects "No": nothing else happens.
  - If user selects "Yes": the app refreshes using the new preferences and updates "Today's Log".

- Implementation details:
  - Added a dedicated confirmation dialog state and pending payload state.
  - Added yes/no handlers for the post-save decision.
  - Extended `requestMealPlan` with options to support:
    - regular meal-card generation flow
    - silent refresh + auto-log update flow for "Today's Log"


13) Fixed "Yes Update Today's Log" Not Appearing

- Root cause:
  - "Today's Log" UI uses dynamic meal periods (2/3/4/5/6/7 slots), but logging logic used fixed period IDs (`m1`-`m5`).
  - In some user plans, refreshed meals were saved to period IDs that were not currently rendered.

- Fix in `src/pages/NutritionDemo.jsx`:
  - Added `resolveVisiblePeriodId(hour, periodDefinitions)` helper.
  - Updated all relevant logging/mapping points to use visible period resolution:
    - initial daily logs mapping
    - current period AI-lock check
    - manual AI log flow
    - post-preferences "Yes, update Today's Log" flow

- Result:
  - When user chooses "Yes" after saving preferences, refreshed meal now updates a visible row in "Today's Log".


14) Added Inspect Endpoint for Saved Nutrition Data + Better Refresh Error Visibility

- Added a new private endpoint to inspect exactly what is saved for the logged-in user:
  - `GET /api/users/nutrition-preferences`
  - Returns:
    - `liked_foods`
    - `disliked_foods`
    - `nutrition_preferences`

- Files updated:
  - `server/controllers/userController.js`
  - `server/routes/userRoutes.js`

- Updated `src/pages/NutritionDemo.jsx` refresh flow:
  - Uses a stable fallback period list when dynamic `periods` state is not ready yet.
  - Adds explicit success/failure browser alerts for the "Yes, update Today's Log" path so silent failures are visible during debugging.


15) Exported Current Users Snapshot (No Password Hashes)

- On request, exported all users data from MongoDB (excluding password hashes) to:
  - `all_users_no_password.json` (project root)
- Purpose: direct inspection/debugging of saved user data in IDE.


16) Fixed NutritionDemo TS Errors Around Dialog/Textarea Components

- In `src/pages/NutritionDemo.jsx`, added local `any`-typed aliases for UI primitives:
  - `DialogContentAny`, `DialogHeaderAny`, `DialogTitleAny`, `TextareaAny`
- Replaced direct JSX usage in this page with those aliases.

- Reason:
  - With `checkJs`, some UI primitives imported from JS-based components were inferred too narrowly by TS in this file (children/value/className prop errors).

- Result:
  - Removed the reported `DialogTitle`/dialog block type errors in `NutritionDemo.jsx` without changing runtime behavior.


17) Fixed Profile.jsx TS Errors (Dialog/Input/Select)

- In `src/pages/Profile.jsx`:
  - Added local `any` aliases for UI primitives used in the file:
    - `DialogContentAny`, `DialogHeaderAny`, `DialogTitleAny`
    - `InputAny`
    - `SelectAny`, `SelectTriggerAny`, `SelectValueAny`, `SelectContentAny`, `SelectItemAny`
  - Replaced direct JSX usage in dialog/edit blocks with those aliases.
  - Adjusted `editValue` state to allow `string | number` for number-input flow.
  - Marked `profileSections` as `any[]` to avoid strict-union inference errors under `checkJs`.

- Result:
  - Cleared the reported TS errors in `Profile.jsx` (including the dialog block errors around lines ~342-344 and related profile edit dialog type issues), without changing runtime behavior.


18) Nutrition Preference Conflict Rules: Latest Wins + Cross-List Cleanup + User Clarification

- Implemented rule-based conflict resolution for free-text preference extraction:
  - Latest statement wins between like/dislike for the same item.
  - Works for mixed/compound text (e.g. "I like X, but I dislike X" â†’ dislike wins).

- Added cross-list reconciliation across:
  - `nutrition_preferences.soft_likes`
  - `nutrition_preferences.soft_dislikes`
  - `liked_foods`
  - `disliked_foods`

- Behavior:
  - Incoming dislikes remove matching likes.
  - Incoming likes remove matching dislikes.
  - Duplicates are deduped.
  - Overlaps are cleaned consistently.

- Added clarification-question output in extractor response:
  - Backend now returns `clarification_questions` for contradictory statements.
  - Frontend `NutritionDemo` save flow now asks user to confirm final preference (like/dislike) and re-submits confirmation text to extractor.

- Updated backend endpoint behavior:
  - `PUT /api/users/nutrition-preferences/extract` now also updates:
    - `liked_foods`
    - `disliked_foods`
  - Response now includes updated:
    - `nutrition_preferences`
    - `liked_foods`
    - `disliked_foods`
    - `clarification_questions`

- Tests:
  - Updated/added pipeline tests for:
    - latest statement wins
    - same-text contradiction handling
    - cross-list like/dislike reconciliation


19) Country-Based Default Currency in Extractor Save Flow

- Added backend countryâ†’currency resolver utility:
  - `server/utils/currencyByCountry.js`
  - Supports country code from request/profile/locale/accept-language fallback.
  - Includes eurozone mapping and common currency defaults (ILS, USD, GBP, EUR, etc.).

- Updated `PUT /api/users/nutrition-preferences/extract` flow:
  - Reads optional `country_code` from request body.
  - If user did not explicitly provide a currency in text, assigns default currency from detected country context.
  - Keeps explicit user currency if provided.

- Updated frontend `NutritionDemo`:
  - Detects browser country code from locale.
  - Sends `country_code` with extractor save request (including clarification re-submit step).

- Added tests:
  - `server/tests/currencyByCountry.test.js`
  - Verifies ILâ†’ILS, USâ†’USD, FRâ†’EUR, and context/accept-language resolution.


20) Added Time-Reference Nutrition Preferences (Day Rules + Meal-Time Rules)

- Extended nutrition preferences schema to support time-based needs explicitly:
  - `nutrition_preferences.rule_based_preferences.day_rules[]`
    - `day_of_week` (`sunday`...`saturday`)
    - `rule_type` (`cheat_day`, `budget_flex`, `fasting`, `custom`)
    - `note`
  - `nutrition_preferences.rule_based_preferences.meal_time_rules[]`
    - `meal_period` (`breakfast`, `lunch`, `afternoon_snack`, `dinner`, `evening_snack`)
    - `preference` (`light`, `moderate`, `heavy`, `high_protein`, `low_carb`)
    - `max_calories`
    - `note`

- Updated models:
  - `server/models/User.js`
  - `ai-service/models/User.js`

- Updated extractor pipeline:
  - `server/services/nutritionPreferences/extractionService.js`
    - Added parsing for day-based rules from free text (e.g. cheat day on Saturday, fasting day).
    - Added parsing for meal-time preference rules (e.g. light evening meal, high-protein breakfast).
  - `server/services/nutritionPreferences/normalizationLayer.js`
    - Added normalization/dedup for `day_rules` and `meal_time_rules`.
  - `server/services/nutritionPreferences/validator.js`
    - Added strict schema validation for both new rule arrays.

- Updated meal-generation context:
  - `ai-service/prompts/mealPrompt.js`
  - Structured preferences summary now includes `day_rules` and `meal_time_rules`, so meal generation can consider those constraints.

- Tests:
  - Updated `server/tests/nutritionPreferencesPipelineService.test.js` with a realistic case validating extraction of:
    - `day_rules` (`saturday` + `cheat_day`)
    - `meal_time_rules` (`evening_snack` + `light` + `max_calories`)
  - Test suite passes.


21) Implemented Phase 1: LLM Extractor + Constrained Reviewer + Deterministic Decision Engine + Audit Queue

- Added a new modular pipeline under:
  - `server/services/nutritionPreferencePhase1/`

- New module structure:
  - `schemaContract.js`
  - `llmStructuredJsonClient.js`
  - `extractorModule.js`
  - `normalizationModule.js`
  - `validationModule.js`
  - `reviewerModule.js`
  - `decisionEngine.js`
  - `auditLogger.js`
  - `programmerReviewQueue.js`
  - `pipelineService.js`
  - `index.js`
  - `rules/` (safe rule layer files)

- Safe external rule layer files added (editable, non-core):
  - `rules/fewShotExamples.json`
  - `rules/normalizationDictionary.json`
  - `rules/foodSynonyms.json`
  - `rules/promptClarifications.json`
  - `rules/ambiguityRules.json`
  - `rules/noteRoutingRules.json`
  - `rules/conflictDetectionHints.json`

- New protected endpoint added:
  - `PUT /api/users/nutrition-preferences/extract-phase1`
  - Files:
    - `server/controllers/userController.js`
    - `server/routes/userRoutes.js`

- Endpoint behavior:
  - Runs LLM extractor module (JSON-only extraction output shape).
  - Runs deterministic normalization + validation.
  - Runs constrained reviewer module (LLM if configured, deterministic fallback otherwise).
  - Runs deterministic final decision engine with exactly one state:
    - `AUTO_SAVE`
    - `ASK_USER`
    - `DO_NOT_SAVE`
    - `SAVE_FOR_PROGRAMMER_REVIEW`
  - Saves only on `AUTO_SAVE`; otherwise no DB write.

- Logging / audit:
  - JSONL audit log appended per request:
    - default path: `server/logs/nutrition_extraction_audit.jsonl`
  - Programmer review queue JSONL for hard/rejected cases:
    - default path: `server/logs/nutrition_programmer_review_queue.jsonl`
  - Paths can be overridden by env:
    - `NUTRITION_AUDIT_LOG_PATH`
    - `NUTRITION_PROGRAMMER_QUEUE_PATH`

- Important safety boundaries implemented:
  - No schema/model changes in this phase.
  - No enum changes.
  - No validator core mutation by reviewer.
  - No merge/update logic mutation by reviewer.
  - No auto-delete of existing data from reviewer.
  - Reviewer can only emit `rule_suggestions` proposals for safe rule-layer updates.

- Tests added:
  - `server/tests/nutritionPreferencePhase1.pipeline.test.js`
  - Covers:
    - AUTO_SAVE valid grounded extraction
    - ASK_USER for ambiguity
    - DO_NOT_SAVE invalid shape
    - SAVE_FOR_PROGRAMMER_REVIEW low confidence
    - contradiction (`vegan` + `salmon`)
    - mixed Hebrew-English schedule rule (`Saturday indulge`, dinner light)


22) Frontend Integration Fixes: Phase1 Extract Endpoint + Meal Update Fallback

- Updated `src/pages/NutritionDemo.jsx` to use Phase 1 extractor endpoint first:
  - `PUT /users/nutrition-preferences/extract-phase1`
  - Falls back to legacy `PUT /users/nutrition-preferences/extract` only if Phase1 route is unavailable (404).

- Added Phase1 decision handling in frontend:
  - If `ASK_USER`, prompts user for clarification and retries extraction with clarification text.
  - If final decision is not `AUTO_SAVE`, shows a clear save error and does not continue silently.

- Fixed/strengthened meal update flow when user clicks:
  - "Yes, update Today's Log"
  - In `requestMealPlan`, primary generator remains `aiApi.post('/meal/next', payload)`.
  - Added fallback generator:
    - `api.post('/nutrition/meal-plan', fallbackPayload)`
    - Used automatically if AI service fails.

- Improved logging robustness for Today's Log update:
  - Guaranteed period fallback ID if dynamic period resolution fails.
  - Success alert now indicates when fallback generator was used.

- Validation checks run:
  - `npx eslint src/pages/NutritionDemo.jsx --quiet`
  - `node --test server/tests/nutritionPreferencePhase1.pipeline.test.js`
  - `node --test server/tests/nutritionPreferencesPipelineService.test.js`


23) Fixed Phase1 Validation Edge Case for Hebrew Dislike Input

- Issue observed:
  - User input like `×× ×™ ×œ× ××•×”×‘ ×˜×•×¤×•` could return:
    - `Validation failed for normalized proposed_update.`
  - Root cause was an over-strict Phase1 validation path that treated some non-structural delete-request formatting issues as full schema invalidation.

- Fixes:
  - `server/services/nutritionPreferencePhase1/normalizationModule.js`
    - Normalized output now always has consistent shape:
      - `{ nutrition_preferences: {} }` at minimum.
  - `server/services/nutritionPreferencePhase1/validationModule.js`
    - Validation now separates:
      - schema/structure errors (hard invalid)
      - delete-request issues (kept as safety signal, not structural schema failure)
  - `server/services/nutritionPreferencePhase1/decisionEngine.js`
    - Added explicit branch for "no structured update extracted" ×’â€ â€™ `ASK_USER` (instead of hard fail).

- New regression test:
  - `server/tests/nutritionPreferencePhase1.pipeline.test.js`
  - Added case:
    - Hebrew text `×× ×™ ×œ× ××•×”×‘ ×˜×•×¤×•`
    - Ensures it does not fall into `DO_NOT_SAVE` due validation edge-case.

- Result:
  - Phase1 test suite passes (7/7).


24) Added Live Auto-Updating Snapshot File for `aurh10@gmail.com`

- Goal:
  - Keep one fixed file in project root that you can open anytime and see current saved data for `aurh10`.

- Added utility:
  - `server/utils/liveUserSnapshot.js`
  - Tracks one email (default): `aurh10@gmail.com`
  - Writes sanitized user snapshot (without password) to:
    - `aurh10_live_snapshot.json` (project root)
  - Env overrides supported:
    - `LIVE_SNAPSHOT_EMAIL`
    - `LIVE_SNAPSHOT_PATH`

- Integrated auto-write hook in `server/controllers/userController.js`:
  - Runs after successful saves in:
    - register (for tracked user)
    - profile update
    - change password
    - food preference update
    - legacy nutrition extraction save
    - phase1 nutrition extraction save (`AUTO_SAVE`)
    - forgot-password reset save
  - Fail-safe: snapshot write errors are logged and do not break API response.

- Created initial snapshot file now:
  - `aurh10_live_snapshot.json`
  - This file will refresh automatically on next relevant user save events.


25) Simplified Phase1 Extractor Decision Flow to Reduce False Failures

- Problem:
  - Extractor path could feel "not working" because decision layer was too strict.
  - Common LLM output shapes were rejected too easily.

- Simplifications applied:
  - `server/services/nutritionPreferencePhase1/validationModule.js`
    - Accepts `proposed_update` when LLM returns direct nutrition category keys
      (e.g. `soft_dislikes`) without mandatory wrapping under `nutrition_preferences`.
    - Still blocks truly unknown/unsafe top-level keys.

  - `server/services/nutritionPreferencePhase1/decisionEngine.js`
    - No longer forces `ASK_USER` for every conflict/delete signal.
    - `ASK_USER` now mainly for:
      - explicit ambiguities
      - contradictions
      - invalid delete-request issues
    - For clear statements, allows `AUTO_SAVE` and returns warning-style reasons.

- New regression test:
  - `server/tests/nutritionPreferencePhase1.pipeline.test.js`
  - Added case:
    - direct `proposed_update.soft_dislikes.foods = ['tofu']`
    - verifies this now results in `AUTO_SAVE`.

- Result:
  - Phase1 test suite passes (8/8).


26) Fixed Soft Like/Dislike Conflict Cleanup for Variant Phrases (e.g. "×˜×•×¤×•" vs "×˜×•×¤×• ×‘×›×œ×œ")

- Problem:
  - Existing saved data could keep contradictory overlap in:
    - `soft_likes.foods`
    - `soft_dislikes.foods`
  - Especially when one side had expressive variants like:
    - `"×˜×•×¤×•"` vs `"×˜×•×¤×• ×‘×›×œ×œ"`

- Fix implemented:
  - `server/services/nutritionPreferences/mergeService.js`
  - Strengthened key normalization for preference comparison:
    - strips punctuation/noise words
    - normalizes expressive modifiers (Hebrew + English)
    - dedupe now uses normalized key for string arrays
  - This makes overlap resolution deterministic and removes like-side when the same item appears in dislike-side.

- Added auto-repair on nutrition preference fetch:
  - `server/controllers/userController.js` (`GET /api/users/nutrition-preferences`)
  - On read, backend now repairs stale overlap with deterministic merge cleanup and saves if changed.
  - Live snapshot file updates after repair for tracked user.

- Added regression test:
  - `server/tests/nutritionPreferencesPipelineService.test.js`
  - New case verifies overlap cleanup:
    - like `"×˜×•×¤×•"` + dislike `"×˜×•×¤×• ×‘×›×œ×œ"` => like removed, dislike kept.

- One-time local repair applied:
  - `aurh10_live_snapshot.json` was repaired so `soft_likes.foods` no longer contains tofu when tofu exists in `soft_dislikes`.


27) Fixed "Extractor failed to parse input" for Simple Hebrew Text (LLM Fallback)

- Problem observed:
  - For simple inputs like:
    - `×× ×™ ××•×”×‘ ×˜×•×¤×•`
  - Phase1 could return:
    - `Please clarify: Extractor failed to parse input.`
  - Root cause:
    - LLM extractor call failing/unavailable without deterministic fallback.

- Fix:
  - `server/services/nutritionPreferencePhase1/extractorModule.js`
  - Added deterministic fallback extraction using existing parser:
    - `extractNutritionPreferencesFromText(...)`
  - Behavior now:
    - If LLM is not configured or call fails:
      - fallback parser is used
      - structured extraction still proceeds
      - no hard parse-failure user message for simple text

- New regression test:
  - `server/tests/nutritionPreferencePhase1.pipeline.test.js`
  - Added test:
    - `"×× ×™ ××•×”×‘ ×˜×•×¤×•"` with no LLM client configured
    - verifies `AUTO_SAVE` via fallback path.

- Result:
  - Phase1 test suite passes (9/9).

28) Rolled Back "Tell Me What You Like" to Regular Extractor + Fixed Hebrew Conflict Cleanup

- Why:
  - Phase1 route was saving only `nutrition_preferences`, while meal flow still depends on `liked_foods` / `disliked_foods`.
  - This caused cases like `àğé ìà àåäá èåôå` to look broken in behavior.
  - Existing stale values also contained noisy phrase tokens and overlap.

- Frontend rollback:
  - File: `src/pages/NutritionDemo.jsx`
  - `handleSaveNutritionPreferences` now calls only:
    - `PUT /api/users/nutrition-preferences/extract`
  - It no longer uses `extract-phase1` as primary path.

- Backend conflict and normalization fixes:
  - File: `server/services/nutritionPreferences/mergeService.js`
    - Added stronger normalization for preference keys (Hebrew/English phrase stripping).
    - Added canonical food alias mapping for cross-language collisions:
      - `èåôå <-> tofu`, `àáå÷ãå <-> avocado`, `ùå÷åìã <-> chocolate`.
    - Added final normalization pass + second soft-like/soft-dislike resolution pass to remove stale overlaps.
  - File: `server/services/nutritionPreferences/normalizationLayer.js`
    - Rebuilt `cleanPreferenceFoodToken` with Unicode-safe Hebrew regexes.
    - Cleans preference phrases/noise words and punctuation before saving foods.
  - File: `server/services/nutritionPreferences/extractionService.js`
    - Improved note-cue logic to avoid false positive soft-like notes on pure dislike text.
  - File: `server/services/nutritionPreferences/index.js`
    - Fixed conflict question path bug (`liked_foods` vs `disliked_foods` exact match).
    - Added same canonical food alias mapping in list merge/conflict keys.

- Test coverage:
  - File: `server/tests/nutritionPreferencesPipelineService.test.js`
  - Added regression test for cross-language tofu conflict:
    - Existing like `tofu` + incoming Hebrew dislike `èåôå` =>
      - removed from likes
      - moved to dislikes


29) Added Time-Based Nutrition Dataset + Meal Agent Context Filtering

- Why:
  - You asked for a dedicated nutrition-time dataset with notes by day and by meal type, so meal generation can use relevant rules for the current day/meal.

- New schema field added:
  - `nutrition_preferences.rule_based_preferences.time_context_notes`
  - Structure per item:
    - `day_of_week`: one of week days or `any`
    - `meal_period`: one of meal periods or `any`
    - `note`: free text note

- Updated files:
  - `server/models/User.js`
  - `ai-service/models/User.js`
  - `server/services/nutritionPreferences/validator.js`
  - `server/services/nutritionPreferences/normalizationLayer.js`
  - `server/services/nutritionPreferences/extractionService.js`
  - `ai-service/prompts/mealPrompt.js`
  - `ai-service/controllers/mealController.js`
  - `src/pages/NutritionDemo.jsx` (sends `day_of_week` in meal request)

- Extraction support:
  - Parser now captures time-context notes from text segments that mention a day and/or meal period.
  - Example extraction:
    - "On Saturday dinner keep it light" -> `day_of_week: saturday`, `meal_period: dinner`, with note.
    - "In the evening keep meals easy" -> `day_of_week: any`, `meal_period: evening_snack`, with note.

- Meal-agent behavior change:
  - Prompt now injects a dedicated "Current time-context preferences" section.
  - It filters notes/rules by current day + meal period (including `any`) and marks them highest priority for current meal generation.

- Snapshot cleanup:
  - Cleaned stale duplicated preference strings in:
    - `aurh10_live_snapshot.json`
    - `all_users_no_password.json`

- Tests:
  - Added regression test:
    - `extracts time_context_notes for day and meal period guidance`
  - Suites passing:
    - `server/tests/nutritionPreferencesPipelineService.test.js` (12/12)
    - `server/tests/nutritionPreferencePhase1.pipeline.test.js` (9/9)


30) Added Explicit Day/Meal Notes Dataset + Fixed "àğé àåäá â'çğåï áùáú" Parsing

- Issue you reported:
  - `"àğé àåäá â'çğåï áùáú"` was not mapped correctly:
    - food token became messy
    - Saturday note was missing
  - You also asked to always have visible notes nodes for:
    - all 7 days
    - all 5 meal periods

- Core fixes:
  - Rebuilt `normalizationService` with Unicode-safe Hebrew parsing:
    - robust day aliases including forms like `áùáú`
    - safer list splitting that no longer breaks foods with apostrophe/geresh like `â'çğåï`
  - Improved food token cleaning in `normalizationLayer`:
    - removes day/time words from food tokens
    - keeps apostrophe in food names (so `â'çğåï` remains intact)
  - Added extraction of explicit time notes map in `extractionService`:
    - `rule_based_preferences.time_notes.by_day`
    - `rule_based_preferences.time_notes.by_meal_period`
  - Kept `time_context_notes` and `day_rules` for richer context.

- New dataset (always shaped):
  - `nutrition_preferences.rule_based_preferences.time_notes`:
    - `by_day`: sunday..saturday
    - `by_meal_period`: breakfast, lunch, afternoon_snack, dinner, evening_snack
  - Added to both schemas:
    - `server/models/User.js`
    - `ai-service/models/User.js`
  - Added to validation + normalization:
    - `server/services/nutritionPreferences/validator.js`
    - `server/services/nutritionPreferences/normalizationLayer.js`

- Controller behavior:
  - `userController` now enforces full `time_notes` shape on save/repair so these nodes are always present.

- Meal agent behavior:
  - Prompt now reads both:
    - `time_notes` (day/meal maps)
    - `time_context_notes`
  - It filters them by current day + meal period and marks them highest priority.

- Verified result for exact phrase:
  - Input: `àğé àåäá â'çğåï áùáú`
  - Output includes:
    - `soft_likes.foods: ["â'çğåï"]`
    - `rule_based_preferences.time_notes.by_day.saturday: "àğé àåäá â'çğåï áùáú"`
    - `rule_based_preferences.time_context_notes` with `day_of_week: saturday, meal_period: any`

- Snapshot files updated to show new nodes immediately:
  - `aurh10_live_snapshot.json`
  - `all_users_no_password.json`

- Tests:
  - Added regression test:
    - `hebrew day preference keeps food token and writes saturday time notes`
  - Nutrition pipeline suite now passes (13/13)

31) Applied Your Exact Example to `aurh10_live_snapshot.json` for Verification

- Ran the updated extractor pipeline with:
  - `àğé àåäá â'çğåï áùáú`
- Snapshot now shows:
  - `soft_likes.foods` includes `â'çğåï`
  - `rule_based_preferences.day_rules` has Saturday custom rule
  - `rule_based_preferences.time_context_notes` has Saturday + any meal note
  - `rule_based_preferences.time_notes.by_day.saturday` contains the same note


32) Conflict Flow Fixed + Free-Text No Longer Adds to Game Lists

- Your requirement:
  - `Tell me what you like` must NOT add items into:
    - `liked_foods`
    - `disliked_foods`
  - It can only remove from these lists on confirmed conflict.
  - If user reverses preference (like -> dislike), ask confirmation first, then update.

- Backend changes:
  - `server/services/nutritionPreferences/index.js`
    - Removed auto-add behavior in `mergeFoodPreferenceLists`.
    - Free-text extraction now only removes conflicting items from opposite game list.
  - `server/controllers/userController.js` (`PUT /api/users/nutrition-preferences/extract`)
    - Added `confirm_conflicts` flag support.
    - If conflicts/clarification questions exist and `confirm_conflicts !== true`:
      - return `requires_confirmation: true`
      - do NOT save yet.
    - Save happens only after confirmation request.

- Frontend changes:
  - `src/pages/NutritionDemo.jsx`
    - `runExtractorRequest` now supports `{ confirmConflicts: true }`.
    - After user answers conflict question(s), it re-sends confirmation text with `confirm_conflicts: true`.

- Behavior now:
  - Example: existing `liked_foods` has tofu, user writes `I don't like tofu`.
  - System asks confirmation first.
  - On confirmation:
    - removes tofu from `soft_likes`
    - removes tofu from `liked_foods`
    - adds tofu to `soft_dislikes`
    - does NOT auto-add tofu into `disliked_foods`.

- Tests updated and passing:
  - `server/tests/nutritionPreferencesPipelineService.test.js`
    - updated expectations for no auto-add to game lists
    - added `free-text extraction does not add new liked/disliked game items`
  - Suites:
    - nutrition preferences tests: 14/14 pass
    - phase1 pipeline tests: 9/9 pass


33) Fixed "Create Meal" Availability + Added Full Meal Decision Context (Nutrition + Workout + Time + User Text)

- Problem fixed:
  - After using "Tell me what you like", creating a meal could feel blocked because the button was disabled when current period already had an AI meal.

- UI fix:
  - `src/pages/NutritionDemo.jsx`
    - Removed hard-block guard in `openMealRequestDialog`.
    - Removed `disabled={currentPeriodHasAIMeal}` from Plan Meal button.
    - Updated helper text when an AI meal already exists to:
      - `Will replace current AI meal`.

- Added workout context into meal request payload:
  - `src/pages/NutritionDemo.jsx`
  - Payload now includes `workout_context` from user profile fields:
    - goal, experience_level, workout_days_per_week, session_duration,
      environment, activity_level, injuries,
      workout_plan_status, workout_plan_source,
      has_existing_plan, trainer_personality.

- AI meal controller upgrade:
  - `ai-service/controllers/mealController.js`
  - Accepts `workout_context` from request.
  - Merges request context with profile-derived workout context.
  - Passes merged context into prompt builder.

- Meal prompt upgrade:
  - `ai-service/prompts/mealPrompt.js`
  - Added workout context summary section in user prompt:
    - `Workout context from profile/session:`
  - Prompt already includes:
    - current time/day/meal period,
    - structured nutrition preferences,
    - time-context nutrition rules,
    - `meal_request_note` text,
    - `nutrition_preferences_note` text.

- Verified:
  - Prompt contains all required decision inputs:
    - nutrition data
    - workout data
    - day/time context
    - user free text
  - Nutrition tests still pass (14/14)
  - Phase1 tests still pass (9/9)


34) Fixed "Generate Meal Does Nothing" Reliability Path

- Root causes addressed:
  - AI request could wait too long before fallback.
  - Local AI auth middleware missed `jsonwebtoken` import, causing auth failure for `/ai/meal/*` routes.

- Changes:
  - `ai-service/middleware/auth.js`
    - Added:
      - `const jwt = require('jsonwebtoken');`
    - This restores valid JWT verification in AI service routes.

  - `src/pages/NutritionDemo.jsx`
    - Meal AI call now uses a per-request timeout of 20s:
      - `aiApi.post('/meal/next', payload, { timeout: 20000 })`
    - Added stronger fallback handling:
      - If AI fails, fallback `/nutrition/meal-plan` is attempted.
      - If fallback also fails, combined failure reason is thrown.
    - Added explicit user feedback on failure:
      - meal error now carries `error_message`
      - alert is shown with the backend reason instead of silent failure.

  - `src/components/nutrition/MealPlanCard.jsx`
    - Error state now displays backend `error_message` when available
      (instead of only generic text).

- Result:
  - "Generate Meal" no longer fails silently.
  - You get clear reason text if both AI and fallback fail.
  - Local AI service auth bug is fixed so `/ai/meal/next` can work correctly with valid token.


35) Fixed Zero-Macro AI Meal Output ("Rest & Reset" Case)

- Issue observed:
  - AI sometimes returned a meal object with `meal_name` but no real nutrition content
    (0 calories / 0 macros / empty or non-useful foods).
  - Frontend previously accepted it because validation only checked `meal_name`.

- Fix implemented in `src/pages/NutritionDemo.jsx`:
  - Added `hasUsableMealContent(meal)` validator.
  - AI response is now rejected if it has no usable meal content.
  - Rejected AI output now triggers automatic fallback generator.
  - Final meal validation now requires:
    - valid object
    - `meal_name`
    - usable nutrition content (not zero/empty meal)

- Result:
  - You should no longer get a fake "meal" with 0 macros.
  - If AI returns empty output, system falls back and returns a real meal.

36) Meal Agent Context Upgrade: Decision Tabs + Accepted Meal History + Safer Fallback Foods

- Goal implemented:
  - The nutrition agent now gets a structured decision context that explicitly tells it what to read before deciding.
  - It now receives previous accepted/saved meals history.

- Backend API additions:
  - `server/controllers/nutritionController.js`
    - Added `getRecentSavedMeals` endpoint handler.
    - New route returns recent saved meals for AI context:
      - `GET /api/nutrition/recent-saved?limit=20&days=45`
    - Response includes meal name, macros, date, and foods (sanitized).
  - `server/routes/nutritionRoutes.js`
    - Added route: `/recent-saved`.

- Frontend payload upgrade:
  - `src/pages/NutritionDemo.jsx`
    - Added `fetchAcceptedMealHistory()`.
    - Before generating meal, frontend fetches recent saved meals and sends them in:
      - `accepted_meal_history`

- AI controller upgrade:
  - `ai-service/controllers/mealController.js`
    - Accepts `accepted_meal_history` from request body.
    - Passes it into meal prompt builder.

- Prompt/agent upgrade:
  - `ai-service/prompts/mealPrompt.js`
    - Added explicit “Decision tabs to read before creating this meal (in order)”.
    - Added a dedicated section for previous accepted/saved meals history.
    - Added stronger rules:
      - do not output empty meals
      - do not use context/day sentences as food names
    - Added normalization/filtering for noisy soft-like/soft-dislike food tokens.

- Fallback generator hardening:
  - `server/controllers/nutritionController.js`
    - Filters invalid liked foods (empty names, noisy tokens, no nutrition signal).
    - Skips zero-nutrition foods in fallback meal assembly.
    - Prevents bad outputs like phrase-as-food or zero-calorie placeholder food lines.

- Validation run:
  - `node --check server/controllers/nutritionController.js` passed
  - `node --check server/routes/nutritionRoutes.js` passed
  - `node --check ai-service/controllers/mealController.js` passed
  - `node --check ai-service/prompts/mealPrompt.js` passed
  - `npx eslint src/pages/NutritionDemo.jsx --quiet` passed

37) Added Meal Suggestion Actions: "Give me recap" + "Something else" (High Priority Request)

- Added two new actions on every meal suggestion card:
  - `Give me recap`
  - `Something else`

- UI changes:
  - `src/components/nutrition/MealPlanCard.jsx`
    - New action buttons in the meal card footer.
    - `Give me recap` calls parent callback.
    - `Something else` opens a follow-up flow.
    - Also hardened food rendering to avoid crash if `meal.foods` is missing.

- Recap flow:
  - `src/pages/NutritionDemo.jsx`
    - Added `handleMealRecap(meal)`.
    - Shows a quick recap popup with:
      - meal name
      - macros
      - main foods
      - last request context
      - whether high-priority mode was applied.

- "Something else" flow:
  - `src/pages/NutritionDemo.jsx`
    - Added dialog with free text input (`showMealRefineDialog`, `mealRefineText`).
    - On submit, re-generates meal with:
      - `meal_request_priority: 'high'`
      - the new free-text request.

- High-priority request path to the agent:
  - Frontend now sends `meal_request_priority` in meal generation payload.
  - `ai-service/controllers/mealController.js` now forwards this field to prompt builder.
  - `ai-service/prompts/mealPrompt.js` now includes:
    - request priority in prompt context
    - explicit instruction to give high weight when priority is HIGH
      (unless it conflicts with hard restrictions).

- Extra reliability fix done in same change:
  - `Try Again` action no longer risks passing a click event object into `requestMealPlan`.
  - Request text handling now safely string-normalizes before `.trim()`.

- Validation:
  - `npx eslint src/pages/NutritionDemo.jsx --quiet` passed
  - `npx eslint src/components/nutrition/MealPlanCard.jsx --quiet` passed
  - `node --check ai-service/prompts/mealPrompt.js` passed
  - `node --check ai-service/controllers/mealController.js` passed

38) Fixed "Something else" UX Flow to Always Open Text Input and Regenerate by User Text

- Your requested behavior is now enforced:
  - Clicking `Something else` closes the current meal card and opens a dedicated text dialog.
  - User writes extra meal instructions.
  - On submit, agent generates another meal based on this text with high priority.

- Implementation:
  - `src/pages/NutritionDemo.jsx`
    - `openSomethingElseDialog()` now closes `showMealPlan` first, then opens refine dialog.
    - `handleSubmitSomethingElse()` validates non-empty text.
    - If text is empty, shows a clear validation alert.
    - If text exists, re-generates meal with:
      - `mealRequestPriority: 'high'`
      - request text from the new dialog.

- Validation:
  - `npx eslint src/pages/NutritionDemo.jsx --quiet` passed

39) Added Multi-Option Meal Selection (up to 3) + Enforced "Choose Option Before Recap/Save"

- Your requested behavior implemented:
  - You can compare up to 3 meal options.
  - You must choose one option before `Give me recap` and `Save` become available.

- How it works now:
  - First generation creates option #1.
  - `Something else` adds another generated option (high-priority request) instead of replacing.
  - Maximum options kept: 3.
  - When there are multiple options, user must click one option card first.
  - Until selection is made:
    - `Give me recap` is disabled
    - `Save` is disabled

- Files changed:
  - `src/components/nutrition/MealPlanCard.jsx`
    - Added option selector UI (Option 1/2/3).
    - Added selection gating logic for recap/save.
    - Added max-3 behavior messaging and disabled `Something else` at 3 options.
  - `src/pages/NutritionDemo.jsx`
    - Added state for `mealOptions` and `selectedMealOptionIndex`.
    - Added `appendAlternative` flow in `requestMealPlan`.
    - `Something else` now appends alternative option (up to 3).
    - Added `handleSelectMealOption` callback.
    - Cleans meal options/selection on close/save/auto-log completion.

- Validation:
  - `npx eslint src/components/nutrition/MealPlanCard.jsx --quiet` passed
  - `npx eslint src/pages/NutritionDemo.jsx --quiet` passed

40) Clarified Meal Card: One Option vs Foods Inside It

- User confusion fixed in UI copy:
  - It now explicitly states when the user is viewing one meal option.
  - It clarifies that the lines below are foods inside the same meal.

- If multiple options exist:
  - Card now shows: "You are viewing meal option X of Y".

- Added heading before ingredient rows:
  - "Foods in this meal"

- File changed:
  - `src/components/nutrition/MealPlanCard.jsx`

- Validation:
  - `npx eslint src/components/nutrition/MealPlanCard.jsx --quiet` passed

41) Default Meal Generation Now Produces 2 Options + Card Shows Meal Description (No Ingredient List)

- Updated behavior:
  - First meal generation now targets 2 options by default.
  - Option 1 and Option 2 are generated from the agent (second option asks for a distinctly different alternative).
  - User must select one option before recap/save (as requested earlier).

- Frontend generation flow changes:
  - `src/pages/NutritionDemo.jsx`
    - Added `targetOptions` option to `requestMealPlan`.
    - `handleMealRequestSubmit` now calls with `targetOptions: 2`.
    - Added internal `generateSingleMealCandidate()` helper in request flow.
    - Generates second alternative (high-priority alternative prompt) when requested.

- Meal card presentation changes:
  - `src/components/nutrition/MealPlanCard.jsx`
    - Removed ingredient row-list view for main presentation.
    - Added `Meal description` block (plain-text meal description).
    - Uses `meal_description` when available, otherwise creates a short narrative from food items.
    - Clarified copy: "This is one full meal option."

- Validation:
  - `npx eslint src/components/nutrition/MealPlanCard.jsx --quiet` passed
  - `npx eslint src/pages/NutritionDemo.jsx --quiet` passed

42) Implemented Current Extractor + Designated Editor Agent (Safe Rollback Included)

- Objective completed:
  - Kept the current extractor logic.
  - Added a post-extractor editor agent that can refine extractor JSON.
  - Added fast rollback switch to extractor-only behavior.

- New files:
  - `server/services/nutritionPreferences/editorAgent.js`
    - Runs constrained LLM editor after extractor.
    - Reads: raw text, extractor partial update, existing preferences, detected conflicts, strict schema contract.
    - Returns structured result:
      - decision (`SAVE` / `ASK_USER` / `DO_NOT_SAVE`)
      - edited update JSON
      - targeted questions
      - confidence and reasons.
    - Uses feature flag:
      - `NUTRITION_EDITOR_ENABLED=false` => editor disabled, extractor-only fallback.
  - `server/services/nutritionPreferences/editorAgentSchemaContract.js`
    - Explicit fillable fields contract sent to editor agent.

- Pipeline wiring:
  - `server/services/nutritionPreferences/index.js`
    - Added helper `buildPipelineFromNormalized(...)`.
    - Kept existing extractor path (`buildNutritionPreferenceUpdate`) unchanged in behavior.
    - Added async wrapper `buildNutritionPreferenceUpdateWithEditor(...)`:
      - runs current extractor
      - runs editor agent
      - re-normalizes/validates candidate safely
      - falls back to extractor output if editor output is invalid/unavailable
      - appends targeted clarification questions from editor
      - supports `DO_NOT_SAVE` signal.
    - Added export alias:
      - `buildNutritionPreferenceUpdateLegacy` (same as current extractor pipeline) for easy rollback reference.

- Controller integration:
  - `server/controllers/userController.js`
    - `/api/users/nutrition-preferences/extract` now uses `await buildNutritionPreferenceUpdateWithEditor(...)`.
    - If editor returns `DO_NOT_SAVE`, endpoint returns without saving.
    - If targeted clarification exists and not confirmed, returns `requires_confirmation: true`.
    - Response now includes `editor` payload for transparency/debugging.

- Tests added:
  - `server/tests/nutritionPreferencesEditorAgent.pipeline.test.js`
    - verifies extractor-only fallback when editor is disabled.
    - verifies safe save path when editor provider is unavailable.

- Validation/tests run:
  - `node --check server/services/nutritionPreferences/editorAgent.js` passed
  - `node --check server/services/nutritionPreferences/editorAgentSchemaContract.js` passed
  - `node --check server/services/nutritionPreferences/index.js` passed
  - `node --check server/controllers/userController.js` passed
  - `node --test server/tests/nutritionPreferencesEditorAgent.pipeline.test.js` passed
  - `node --test server/tests/nutritionPreferencesPipelineService.test.js` passed

15) Single Meal Nutrition Agent Prompt Alignment + Search Rule

- Removed the temporary frontend fallback that called the old AI-service meal endpoint.
  - `src/pages/NutritionDemo.jsx` now sends meal generation only through:
    - `POST /api/nutrition/meal-plan`
  - We no longer fall back to `/ai/meal/next` for meal generation.

- Updated the server-side nutrition agent prompt in:
  - `server/services/singleMealPlannerService.js`

- Goal of the prompt change:
  - make the nutrition agent receive the same meal-generation prompt structure that the old direct LLM meal flow used before
  - while keeping the new `BaseLLMRequest` / nutrition-agent architecture

- The single-meal nutrition agent prompt now explicitly includes:
  - JSON-only response rule
  - food names in English
  - Hebrew short `coach_note`
  - realistic portions and real foods
  - no empty meals
  - request-priority behavior
  - the old decision-tab order:
    1. specific meal request
    2. time context
    3. hard restrictions
    4. soft likes/dislikes + liked/disliked foods
    5. practical constraints
    6. rule-based preferences
    7. budget preferences
    8. workout context + remaining macros
    9. previous accepted meals

- Added explicit web-search behavior to the nutrition agent prompt:
  - if the task is hard
  - or the meal idea is weak
  - or confidence is not high
  - then the agent may use `nutrition_web_search`
  - otherwise it should prefer the user snapshot and not search by default

- Enriched the server-side meal context before calling the nutrition agent:
  - full structured nutrition preferences
  - liked foods
  - disliked foods
  - workout context
  - accepted meal history

- Updated the single-meal service test in:
  - `server/tests/singleMealPlannerService.test.js`
  - to verify:
    - the nutrition web search allowlist is still enabled
    - the prompt contains the old meal-generation structure
    - the low-confidence / hard-task search rule is present

16) Fixed Single-Meal Nutrition Agent 400 Failure More Robustly

- Root issue identified:
  - the single-meal nutrition agent prompt had become too large and duplicated the same user context in both the system prompt and the user prompt
  - this made provider-side 400 failures more likely during meal generation

- Updated `server/services/singleMealPlannerService.js` again:
  - reduced the system prompt so it now contains instructions/rules only
  - moved the actual user snapshot / meal-generation data to the user prompt
  - kept the old meal-generation structure and decision order
  - kept the explicit rule:
    - if the task is hard or confidence is not high, the nutrition agent may use `nutrition_web_search`

- Added a nutrition-agent internal compact retry path:
  - if the primary single-meal generation attempt fails with provider 400
  - the same nutritionist agent retries in a compact mode
  - compact mode disables tools and uses a shorter prompt
  - this stays inside the same server-side nutrition-agent architecture
  - it does NOT fall back to the old AI-service meal generator

- Added a second retry condition:
  - if the primary response returns invalid JSON / unparseable meal output
  - the service retries once in compact mode before failing

- Result:
  - single-meal generation is now more resilient against:
    - oversized prompt/provider 400 failures
    - invalid JSON meal responses
    - tool-related provider fragility

- Updated validation/test coverage:
  - `server/tests/singleMealPlannerService.test.js` updated to match the new prompt structure

17) Removed Single-Meal Fallback/Retry Path

- Per request, removed the extra single-meal fallback behavior from:
  - `server/services/singleMealPlannerService.js`

- The single-meal nutrition flow now uses one clean prompt path only:
  - same nutritionist agent
  - same meal-generation prompt path
  - optional `nutrition_web_search` only when needed

- Removed:
  - compact retry mode
  - retry-on-provider-400 path inside `SingleMealPlannerService`
  - retry-on-invalid-JSON path inside `SingleMealPlannerService`

- Also fixed a real bug in the prompt builder:
  - `buildSystemPrompt()` was referencing `currentMealPeriod` without defining it
  - this is now resolved by deriving it from `mealContext.meal_period`

- Result:
  - the meal planner now either succeeds from the main nutrition-agent prompt path or fails honestly
  - no hidden second generation path exists inside the single-meal service anymore

18) Server Gemini Provider Fixed for Normal Meal Prompt Flow

- Strong root-cause fix applied:
  - the server-side Gemini provider was using the OpenAI-compatible Gemini endpoint for normal prompt generation
  - the older working meal flow used direct Gemini generation instead

- Updated `server/ai/providers/geminiChatProvider.js`:
  - if no tools are requested:
    - use direct Gemini `generateContent` style request on the server
  - if tools are requested:
    - keep the existing OpenAI-compatible path

- Why this matters:
  - normal single-meal generation is primarily a prompt-generation task
  - direct Gemini is closer to the older working path and is less likely to reject the request with a generic 400

- Updated `server/controllers/nutritionController.js` meal endpoint:
  - `POST /api/nutrition/meal-plan` now catches failures and returns structured JSON error details
  - this prevents the frontend from only seeing generic Axios text like `Request failed with status code 400`
  - backend now logs:
    - requestId
    - userId
    - status
    - code
    - message

- Result:
  - if meal generation succeeds, it uses the nutrition agent prompt path
  - if it fails, the frontend should now receive the real backend/provider message instead of a useless generic 400

19) Returned Single-Meal Generation to Old AI-Service Path + Added Optional Online Search

- Per request, single-meal generation was switched back to the old AI-service generator.
  - `src/pages/NutritionDemo.jsx` now sends meal generation back to:
    - `POST /ai/meal/next`
  - It no longer uses the server-side `POST /api/nutrition/meal-plan` path for the main meal-generation UI flow.

- Kept the richer/newer meal prompt context in the old generator path:
  - structured nutrition preferences
  - liked/disliked foods
  - workout context
  - accepted meal history
  - meal request text + priority
  - nutrition preference note

- Updated `ai-service/prompts/mealPrompt.js`:
  - added explicit instruction that if the task is hard, the meal idea is weak, or confidence is low, and online search grounding is available, the model may use it before answering
  - also tells the model not to search by default when the user context is already enough

- Updated `ai-service/services/llmChain.js`:
  - the old Gemini path now uses direct Gemini REST `generateContent`
  - added optional Google Search grounding support via Gemini tools
  - when `enableGoogleSearch` is true, the Gemini request includes the search grounding tool
  - if search is not requested and a proxy client exists, the proxy path can still be used

- Updated `ai-service/controllers/mealController.js`:
  - `generateNextMeal` now calls the LLM with `enableGoogleSearch: true`
  - this means the old single-meal generator has access to online search grounding when needed

- Result:
  - meal generation uses the older working AI-service path again
  - prompt/context stays rich
  - Gemini can search online when needed for hard / low-confidence meal generation cases

20) Fixed Local AI Browser Network Path

- Root cause of the latest `Network Error` was likely the frontend calling the AI service directly at:
  - `http://localhost:5002/ai`
  from the browser because of `.env.local`

- Updated `src/api/aiAxios.js`:
  - in local dev (`import.meta.env.DEV`) the frontend now uses:
    - `/ai`
  - this goes through Vite's built-in proxy to `localhost:5002`
  - avoids direct browser cross-origin calls to the AI service port

- Non-dev behavior stays the same:
  - explicit `VITE_AI_URL` can still be used outside dev
  - otherwise it falls back to the deployed AI service URL

- Result:
  - local meal generation / recap calls should now go through the same-origin Vite dev server and proxy cleanly to the AI service

21) Restored Meal Generation To Pre-Agent Path

- Per request, restored the main single-meal generation flow to the way it worked before shifting the request to the server nutrition agent.

- Restored frontend path:
  - `src/pages/NutritionDemo.jsx` uses the old AI-service meal endpoint for meal generation
  - `src/api/aiAxios.js` restored to its original base-URL behavior:
    - `VITE_AI_URL` if defined
    - otherwise deployed AI service URL

- Restored old AI-service generation internals:
  - `ai-service/services/llmChain.js` restored to the original implementation
    - proxy path via `LLM_API_KEY` if available
    - otherwise direct Gemini via `@google/generative-ai`
  - removed the newer Google-search-grounding request shape changes from this file

- Restored old meal endpoint behavior:
  - `ai-service/controllers/mealController.js`
  - `generateNextMeal` now uses the old direct `callWithFallback(systemPrompt, userMessage, 800)` flow again
  - removed the extra search-grounding option and extra wrapped error handling

- Restored old meal prompt behavior:
  - removed the added online-search-specific instructions from `ai-service/prompts/mealPrompt.js`

- Scope:
  - this restore is for the main meal-generation flow only
  - unrelated recap / liked meals / other nutrition UI changes were not reverted here

22) Restored Local AI Endpoint Override For Meal Flow

- Restored `.env.local` back to the local AI-service URL:
  - `VITE_AI_URL=http://localhost:5002/ai`

- This matches the earlier local pre-agent meal-generation setup in the workspace:
  - frontend -> local ai-service -> old meal generator
